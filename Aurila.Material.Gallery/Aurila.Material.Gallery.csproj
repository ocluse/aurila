<Project Sdk="Microsoft.NET.Sdk.BlazorWebAssembly">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>Aurila.Material.Gallery</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Components.WebAssembly" Version="10.0.7" />
    <PackageReference Include="Microsoft.AspNetCore.Components.WebAssembly.DevServer" Version="10.0.7" PrivateAssets="all" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Aurila\Aurila.csproj" />
    <ProjectReference Include="..\Aurila.Material\Aurila.Material.csproj" />
    <ProjectReference Include="..\Aurila.Web\Aurila.Web.csproj" />
  </ItemGroup>

  <!-- Razor library references expose their frontend build metadata as publish items. Those files
       are development inputs, and three identically named copies otherwise collide at publish. -->
  <Target Name="RemoveReferencedFrontendMetadata" BeforeTargets="_HandleFileConflictsForPublish">
    <ItemGroup>
      <ResolvedFileToPublish Remove="@(ResolvedFileToPublish)"
                             Condition="'%(ResolvedFileToPublish.Filename)%(ResolvedFileToPublish.Extension)' == 'package-lock.json' Or '%(ResolvedFileToPublish.Filename)%(ResolvedFileToPublish.Extension)' == 'tsconfig.json'" />
    </ItemGroup>
  </Target>

</Project>
